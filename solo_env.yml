name: solo_env
channels:
  - defaults
  - anaconda
  - bioconda
  - conda-forge
dependencies:
  - defaults::python=3.9
  - nextflow=22.04.0
  - pip
  - pip:
    - scvi-tools
    - scanpy 
    - anndata 
    - argparse
    - pandas

